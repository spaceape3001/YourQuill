#pragma once

namespace yq {
    namespace meta {
    
        /*! \brief Describes a type to be presented as "singular"
        
            The difference between a value & structure is how it presents to the user in a UI (for instance).
        */
        class ValueInfo : public TypeInfo {
        public:
        
        
        };
    }
}
